{{- if .Values.grafanaDashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: flask-app-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  flask-app-monitoring.json: |
    {{ .Files.Get "dashboards/flask-app-monitoring.json" | nindent 4 }}
{{- end }}
